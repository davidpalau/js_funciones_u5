const miArray = [3, 16, 23, 65, 35, 18, 19, 15, 7];
function filtro(miArra, element => {
    var resultado_1 = [];
    var contador = 0;
    miArray.forEach(element => {
        //Filtra los número NO multiplos de 2, o elegir otro número
        if (element % 2 != 0) {
            resultado_2[contador] = element;
            contador++;
            return true
        } else {
            return false;
        }
    });

};


